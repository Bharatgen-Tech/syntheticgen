name: long_context
flow: think_execute

flow_config:
  mode: mode2
  target_output_tokens: 131072
  context_window: 131072
  # Point at the existing instruction.yaml in the original repo.
  # Override via --flow_config (future) or copy into this pipelines/ dir.
  instruction_path: /fsx/pretraining_data/nauman/github/long_context_generation/instruction.yaml
  category: Code
