{"id": "doc_001", "text": "Python's asyncio library provides infrastructure for writing single-threaded concurrent code using coroutines, multiplexing I/O access over sockets and other resources, and running network clients and servers. It provides an event loop, which is the core of every asyncio application — it runs asynchronous tasks and callbacks, performs network I/O operations, and runs subprocesses."}
{"id": "doc_002", "text": "Ray is an open-source unified framework for scaling AI and Python applications like machine learning. It consists of a core distributed runtime and a set of AI libraries for accelerating ML workloads. Ray's distributed runtime is a simple-to-use, minimalist API that makes it easy to parallelize individual Python functions, or even entire Python classes, across a cluster."}
{"id": "doc_003", "text": "PostgreSQL uses Multi-Version Concurrency Control (MVCC) to provide concurrent access to the database and, in memory loaded tables. Under MVCC, each SQL statement sees a snapshot of data (a database version) as it was some time ago, regardless of the current state of the underlying data. This protects the statement from viewing inconsistent data that could be produced by concurrent transactions performing updates on the same data rows."}
